import { SwaggerOptions } from "swagger-ui-express";

const swaggerOptsConfig: SwaggerOptions = {
  customCssUrl: 'https://cdnjs.cloudflare.com/ajax/libs/swagger-ui/5.25.3/swagger-ui.css'
}

export default swaggerOptsConfig